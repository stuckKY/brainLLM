# Brain LLM
This is an AI chatbot which interacts with your existing files.

It requires an existing pgvector database, with values for the ip, port, user and pass provided as environment variables.

OpenAI and Anthropic API keys are passed as environment variables to the container.

Still a work in progress. Claude Code heavily used.