# === Database (external pgvector on Unraid) ===
DB_HOST=192.168.1.100
DB_PORT=5432
DB_USER=brainllm
DB_PASSWORD=changeme
DB_NAME=brainllm

# === API Keys ===
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# === Paths ===
# Absolute path on the host to your documents folder
DOCUMENTS_PATH=/mnt/user/documents/brainllm

# Appdata folder for persistent container storage
APPDATA_PATH=/mnt/user/appdata/brainllm
